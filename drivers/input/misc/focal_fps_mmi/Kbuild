ccflags-$(CONFIG_INPUT_FOCAL_SUPPORT_DRM_SCREEN) += -DCONFIG_DRM_PANEL_NOTIFICATIONS
ccflags-$(CONFIG_INPUT_FOCAL_PANEL_NOTIFICATIONS) += -DCONFIG_PANEL_NOTIFICATIONS

focal_fps_mmi-y += ff_ctl.o plat-msm.o

obj-$(CONFIG_INPUT_FOCAL_FPS_MMI) += focal_fps_mmi.o
